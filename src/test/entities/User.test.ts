import { expect, test, describe } from 'vitest';
import { User } from '../../entities/User';


describe("User fields validation", () => {
    
    test("Email should have a '@' and '.com'",() => {
        const user = new User();

    })
})

